<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.moretion.geodoer"
    android:installLocation="internalOnly"
    android:versionCode="20141112"
    android:versionName="智慧提醒" > <!-- ====================================================================== -->
    <uses-sdk
        android:maxSdkVersion="19"
        android:minSdkVersion="16"
        android:targetSdkVersion="18" />

    <!-- ====================================================================== -->
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <!-- ====================================================================== -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!-- ====================================================================== -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="me.iamcxa.remindme.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <!--
 Maps V2 API
    <uses-permission
        android:name=
        "com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!-======================================================================
    -->
    <application
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:enabled="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/remindme_logo"
        android:largeHeap="true"
        android:logo="@drawable/remindme_logo"
        android:theme="@style/AppTheme" >

        <!-- ================================================================== -->
        <activity
            android:name="tw.geodoer.main.taskList.view.AppMainActivity"
            android:hardwareAccelerated="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ================================================================== -->
        <activity
            android:name="tw.geodoer.main.taskEditor.view.TaskEditorTabFragment"
            android:hardwareAccelerated="true"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.new" />
            </intent-filter>
        </activity>

        <!-- ================================================================== -->
        <provider
            android:name="tw.geodoer.mDatabase.contentProvider.TaskDbProvider"
            android:authorities="tw.moretion.geodoer"
            android:exported="false" >
        </provider>

        <!-- ================================================================== -->
        <activity
            android:name="tw.geodoer.main.taskPreference.view.AppPreferenceActivity"
            android:screenOrientation="portrait" >
        </activity>

        <!-- ================================================================== -->
        <uses-library android:name="com.google.android.maps" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyALO3Tds0aL6qAV4g7zG4FCR8IG25CgGqA" />
        <!-- AIzaSyAwoaXdgj7AAm6YV3i8tZdX3sU6ZucSQec 耀德 Tx300 9/26 -->
        <!-- AIzaSyALO3Tds0aL6qAV4g7zG4FCR8IG25CgGqA 奎研  9/26 -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- ================================================================== -->
        <receiver
            android:name="tw.geodoer.mPriority.receiver.GeoBroadcastReceiver_TaskAlert"
            android:exported="false" >
            <intent-filter>

                <!-- msg -->
                <action android:name="me.iamcxa.remindme.TaskReceiver" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- ================================================================== -->
        <!-- <activity android:name=".provider.AlarmProvider" ></activity> -->


        <!-- ================================================================== -->
        <service android:name="tw.geodoer.mPriority.service.GeoServiceStarter" />
        <service android:name="tw.geodoer.mPriority.service.GeoServiceNotification" />
        <service android:name="tw.geodoer.mPriority.service.GeoServiceEventUpdater" />

        <activity
            android:name="tw.geodoer.main.taskAlert.view.dialog.AlertNotiDialog"
            android:theme="@style/MyDialogStyle"
            android:uiOptions="none" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <service android:name="tw.geodoer.main.taskAlert.controller.AlertHandler" />
        <service android:name="tw.geodoer.main.taskAlert.controller.ActionDelayTheAlert" />
        <service android:name="tw.geodoer.main.taskAlert.controller.ActionFinishTheAlert" />

        <activity
            android:name="tw.geodoer.main.taskList.view.APITestingActivity"
            android:label="@string/title_activity_apitesting" >
        </activity>
        <activity
            android:name="tw.geodoer.main.taskList.view.Main2Activity"
            android:label="@string/title_activity_main2" >
        </activity>
    </application>

</manifest>
